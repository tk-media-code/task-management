---
name: submit-pull-request
description: コミットして送信し（品質チェックは送信時に hook が自動で回す）、Pull Request を作るところまでを行う。作業を終えたときに必ず使う。「push して」「PR 出して」「これで出していい？」「作業完了しました」と言われたら、または自分で完了・修正済みと報告しようとしているときに発動。飛ばすと、チェック未通過のコードが出ていく・Closes # が抜けて Issue が閉じない・勝手にマージしてレビューの機会が失われる、のいずれかが起きる。
---

# 変更を送り出す

**マージはしない。PR を作ったら止まる。**

## 1. 何が出ていくのかを確かめる

```bash
git status
git diff
git log --oneline main..HEAD
```

**意図した変更だけが入っているか**を確認する。意図しないファイル（ローカル設定、
デバッグ用の一時ファイル、生成物）が混ざっていないか見る。

## 2. コミットする

書式は `commit-and-pr` ルールに従う。**件名は日本語・常体・「〜する」で終える。**

```bash
git add <ファイル>
git commit -m "$(cat <<'EOF'
<件名>

<なぜこの変更が必要か。判断の根拠。採らなかった案があればその理由も。>

Closes #<issue番号>

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
```

**本文には「なぜ」を書く。** 何を変えたかは差分を見れば分かる。差分から読み取れないのは
判断の根拠のほうなので、そこを残す。

## 3. 送信する

```bash
git push -u origin <ブランチ名>
```

**既定ブランチへは送らない。** 作業ブランチをそのまま送る。

**品質チェックを手で叩かない。** 送信を検知した hook が `scripts/harness-check.sh` →
`scripts/quality-check.sh` の順に自動で回す。先に手で実行しても同じものが2回走るだけ。

失敗すると**送信そのものがブロックされる。** リモートには何も出ていないので、指摘を修正し、
まだ送信できていないコミットに畳んでから送信し直す。

```bash
# 指摘を修正してから
git add <ファイル>
git commit --amend --no-edit
git push -u origin <ブランチ名>
```

**送信済みのコミットは `--amend` しない。** force push が要るうえ `permissions.deny` で禁止
されている。その場合は修正を新しいコミットとして積む。

**指摘が0件になるまで、この手順を自分で繰り返す。** 安易に作業を止めたり、
ユーザーに丸投げしたりしない。

ブロックを回避する `SKIP_QUALITY_CHECK=1` を、**ユーザーの明示的な指示なしに付けてはならない。**
誤検知やチェック自体の不具合を疑うときは、まずユーザーに状況を説明して判断を仰ぐ。

## 4. PR を作る

```bash
gh pr create --title "<件名>" --body "$(cat <<'EOF'
## 概要

<何を・なぜ変更したか>

Closes #<issue番号>

## 変更内容

- ...

## 確認事項

- [x] ブランチ名にIssue番号を含めている
- [x] 品質チェックを通している
- [ ] 動作確認済み
EOF
)"
```

**`Closes #<issue番号>` を本文に必ず含める。** これが無いとマージしても Issue が閉じない。

## 5. ここで止まる

**`gh pr merge` は実行しない。** マージはユーザーが内容を確認した上で行う。

PR の URL をユーザーに伝え、**確認とマージを依頼して終わる。**

> 完了の目安: PR が作られ、その URL をユーザーに渡している。**マージはしていない。**

## この先

ユーザーがマージしたら `sync-after-merge` スキルへ進む。
