---
name: submit-pull-request
description: 品質チェックを通してからコミット・送信し、Pull Request を作るところまでを行う。作業を終えたときに必ず使う。「push して」「PR 出して」「これで出していい？」「作業完了しました」と言われたら、または自分で完了・修正済みと報告しようとしているときに発動。飛ばすと、チェック未通過のコードが出ていく・Closes # が抜けて Issue が閉じない・勝手にマージしてレビューの機会が失われる、のいずれかが起きる。
---

# 変更を送り出す

**マージはしない。PR を作ったら止まる。**

## 1. 何が出ていくのかを確かめる

```bash
git status
git diff
git log --oneline main..HEAD
```

**意図した変更だけが入っているか**を確認する。意図しないファイル（ローカル設定、
デバッグ用の一時ファイル、生成物）が混ざっていないか見る。

## 2. 品質チェックを通す

```bash
bash scripts/harness-check.sh      # 共通チェック。必ず実行する
bash scripts/quality-check.sh      # プロジェクト固有。存在すれば実行する
```

**指摘が0件になるまで、修正 → 再コミット → 再チェックを自分で繰り返す。**
安易に作業を止めたり、ユーザーに丸投げしたりしない。

**指摘を残したまま送信しない。** この規約は hook で機械的に強制されており、
チェックに失敗すると送信を含むコマンドの実行自体がブロックされる。

ブロックを回避する `SKIP_QUALITY_CHECK=1` を、**ユーザーの明示的な指示なしに付けてはならない。**
誤検知やチェック自体の不具合を疑うときは、まずユーザーに状況を説明して判断を仰ぐ。

## 3. コミットする

書式は `commit-and-pr` ルールに従う。**件名は日本語・常体・「〜する」で終える。**

```bash
git add <ファイル>
git commit -m "$(cat <<'EOF'
<件名>

<なぜこの変更が必要か。判断の根拠。採らなかった案があればその理由も。>

Closes #<issue番号>

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
```

**本文には「なぜ」を書く。** 何を変えたかは差分を見れば分かる。差分から読み取れないのは
判断の根拠のほうなので、そこを残す。

## 4. 送信する

```bash
git push -u origin <ブランチ名>
```

**既定ブランチへは送らない。** 作業ブランチをそのまま送る。

## 5. PR を作る

```bash
gh pr create --title "<件名>" --body "$(cat <<'EOF'
## 概要

<何を・なぜ変更したか>

Closes #<issue番号>

## 変更内容

- ...

## 確認事項

- [x] ブランチ名にIssue番号を含めている
- [x] 品質チェックを通している
- [ ] 動作確認済み
EOF
)"
```

**`Closes #<issue番号>` を本文に必ず含める。** これが無いとマージしても Issue が閉じない。

## 6. ここで止まる

**`gh pr merge` は実行しない。** マージはユーザーが内容を確認した上で行う。

PR の URL をユーザーに伝え、**確認とマージを依頼して終わる。**

> 完了の目安: PR が作られ、その URL をユーザーに渡している。**マージはしていない。**

## この先

ユーザーがマージしたら `sync-after-merge` スキルへ進む。
